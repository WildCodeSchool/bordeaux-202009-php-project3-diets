<button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true"><i class="far fa-times-circle"></i></span>
</button>
<div id="nouvel-evenement">
    <h3 class="part-title">Je propose un événement :</h3>
</div>
<div class="container-fluid-event">
    <div class="row align-items-center justify-content-center">
        <div class="col-7 profile-main-forms">
            <div class="form-event">
                {% form_theme form_event 'bootstrap_4_layout.html.twig' %}
                {{ form_start(form_event) }}
                    {{ form_row(form_event.name) }}
                    {{ form_row(form_event.eventFormat) }}
                    {{ form_row(form_event.dateStart) }}
                    {{ form_row(form_event.dateEnd) }}
                    {{ form_row(form_event.price) }}
                    {{ form_row(form_event.link) }}
                    {{ form_row(form_event.description) }}
                    {{ form_row(form_event.picture) }}
            </div>
            <div class="d-flex flex-row-reverse">
                <button class="btn mt-5">{{ button_label|default('Valider') }}</button>
            </div>
            {{ form_end(form_event) }}
            </div>
        </div>
    </div>
</div>
