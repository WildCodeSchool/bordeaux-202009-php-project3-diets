{% if is_granted ('ROLE_DIETETICIAN') %}
    <button type="button" class="close" data-dismiss="modal" aria-label="Close" xmlns="http://www.w3.org/1999/html">
        <span aria-hidden="true"><i class="far fa-times-circle"></i></span>
    </button>
    <div id="nouvel-evenement">
        <h3 class="part-title">Veuillez vous inscrire sur Stripe (plateforme de gestion de paiement sécurisées) pour créer du contenu payant </h3>
    </div>
    <div>
        <p> Attention ! Votre IBAN et des papiers administratifs vous seront demandés afin de vous verser le bénéfice de vos produits vendus. </br>
            Toutes les informations recueillies par Stripe sont sécurisées et confidentielles, elles ne seront en aucun cas divulguées.
            Voir les Conditions Générales de Vente.
        </p>
    </div>

    <div class="mt-4 d-flex justify-content-center">
        <a href="{{ path('payment_register_stripe',  {'id': app.user.id}) }}" class="btn">S'inscrire sur Stripe pour poster des connaissances ou visio payante</a>
    </div>
    {% else %}
    {% if is_granted ('ROLE_ENTERPRISE') %}
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true"><i class="far fa-times-circle"></i></span>
        </button>
        <div id="nouvel-evenement">
            <h3 class="part-title">Veuillez vous abonner pour créer du contenu</h3>
        </div>
        <div class="d-flex justify-content-center">
            <a id="checkout-subscription" href="#" class="btn">S'abonner</a>
        </div>
        {% block javascripts %}
            {{ encore_entry_script_tags('subscribe') }}
        {% endblock %}
    {% else %}
            {% if app.user %}
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true"><i class="far fa-times-circle"></i></span>
            </button>
            <div id="nouvel-evenement">
                <h3 class="part-title">Veuillez compléter votre profil pour créer du contenu</h3>
            </div>
            <div class="d-flex justify-content-center">
                <a href="{{ path('profile_profil_edit', {'id': app.user.id}) }}" class="btn" >Modifier mon profil </a>
            </div>
            {% else %}
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true"><i class="far fa-times-circle"></i></span>
                    </button>
                    <div id="nouvel-evenement">
                        <h3 class="part-title">Veuillez vous inscrire ou vous connecter pour créer du contenu</h3>
                    </div>
                    <div class="d-flex justify-content-center">
                        <a href="{{ path('app_login') }}" class="btn" >Se connecter</a>
                    </div>
            {% endif %}
    {% endif %}
{% endif %}
