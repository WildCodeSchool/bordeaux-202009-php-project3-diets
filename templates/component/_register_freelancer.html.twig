{% extends 'base.html.twig' %}

{% block title %}Vous êtes auto-entrepreneur{% endblock %}

{% block body %}
    <div class="company-register pt-5 d-flex flex-column">
        <h2 class="pt-5 pb-4 mt-5 text-center">Finalisez votre inscription en tant qu'auto-entrepreneur</h2>
        <div class=" profile-main-forms pt-5 d-flex justify-content-center">
            <div class="form-user w-50">
                {% form_theme form_freelancer 'bootstrap_4_layout.html.twig' %}
                {{ form_start(form_freelancer) }}
                {{ form_row(form_freelancer.name) }}
                {{ form_row(form_freelancer.siret) }}
                {{ form_row(form_freelancer.description) }}
                <button type="submit" class="btn mt-5">Finaliser mon inscription</button>
                {{ form_end(form_freelancer) }}
            </div>
        </div>
    </div>
{% endblock %}
