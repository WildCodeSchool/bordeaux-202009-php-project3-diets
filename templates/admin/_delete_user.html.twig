<form method="post" action="{{ path('admin_delete_user', {'id': listed_user.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer cet utilisateur ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listed_user.id) }}">
    <button class="delete-btn pr-3"><i class="far fa-times-circle"></i></button>
</form>
