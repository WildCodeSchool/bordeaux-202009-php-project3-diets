<form method="post" action="{{ path('profile_delete_resource', {'id': resource.id}) }}" onsubmit="return confirm('Es-tu sûr(e) de vouloir supprimer cette resource ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resource.id) }}">
    <button class="delete-btn col-2"><i class="far fa-times-circle fa-2x justify-content-center"></i></button>
</form>
