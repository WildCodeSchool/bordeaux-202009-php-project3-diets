<form method="post" action="{{ path('profile_delete_service', {'id': service.id}) }}" onsubmit="return confirm('Es-tu sûr(e) de vouloir supprimer ce service ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="delete-btn col-2"><i class="far fa-times-circle fa-2x justify-content-center"></i></button>
</form>
