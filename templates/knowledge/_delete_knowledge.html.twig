<form method="post" action="{{ path('knowledge_delete', {'id': resource.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer cette resource ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resource.id) }}">
    <button class="delete-btn"><i class="far fa-times-circle fa-2x justify-content-center"></i></button>
</form>
